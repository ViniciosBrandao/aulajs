// Entrada de dados
const prompt = require("prompt-sync")();
const raio = Number(prompt("Qual o raio da lata? "));
const altura = Number(prompt("Qual a altura da lata em metros? "));
const custo = 155;
const arquibancada = 30 / 100 * publico * 20;
const cadeiras = 10 / 100 * publico * 40;

// Processamento
const areaLata = (Math.PI * raio ** 2 * 2)
const valorTotal = popular + geral + arquibancada + cadeiras

// Saída da Informação
console.log(`O valor da renda total do jogo é de R$ ${valorTotal.toFixed(2)} reais`);